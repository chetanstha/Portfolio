/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // This is for static export
    images: {
      unoptimized: true, // Disable image optimization
    },
  };
  // aa
  
  module.exports = nextConfig;
  
